'use strict'; 
const { assert, expect } = require('chai');

// describe('Array', () => {
//   let testArr = [1,2,3];

//   describe('indexOf()', () => {
//     it('should be equal to -1', () => {
//       assert.equal(-1, testArr.indexOf(4));
//     });
//   });

//   describe('length', () => {
//     it('should be length of object', () => {
//       assert.equal(3, testArr.length);
//       assert.notEqual(0, testArr.length);
//     })
//   })

// });